import {Colors} from "@/styles/colors";

export enum Buttons {
    Default = `padding: 12px 16px; border-radius: 4px; transition: 0.2s ease-in-out; :hover { background-color: ${Colors.Green600} }; }`,
    LinkDefault = `padding: 12px 16px; background-color: ${Colors.Green700}; color: ${Colors.White}; border-radius: 4px; transition: 0.2s ease-in-out; text-align: center; :hover { background-color: ${Colors.Green600}; } }`,

}
